# Frontend-only build — no backend required.
# Data is stored in the browser (localStorage). These vars are placeholders
# for when a real Supabase backend is wired in later.

# NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=xxxxx
NEXT_PUBLIC_APP_ENV=development
